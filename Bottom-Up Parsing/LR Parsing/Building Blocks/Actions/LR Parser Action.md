# LR Parser Action

An **LR parser action** is an operation performed by an LR parser based on the current [[State|state]] and input [[Symbol|symbol]]. The ACTION table determines which action to perform.