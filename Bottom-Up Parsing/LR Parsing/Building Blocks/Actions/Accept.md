# Accept

Given a parser in a configuration where the [[Accept Item|accept item]] is at the top of the stack and the lookahead symbol is the end-of-input marker $\$$, **accepting** consists in successfully terminating the parsing process, indicating that the input string belongs to the [[Formal Language|language]] generated by the grammar.